- job-template:
    name: 'gate-{name}-buildpackage'

    triggers:
      - zuul

    builders:
      - gerrit-git-prep
      - buildpackage

    publishers:
      - dpkg-buildpackage:
          dscfile: '{name}_*.dsc' 
      - console-log

    node: '{node}'
