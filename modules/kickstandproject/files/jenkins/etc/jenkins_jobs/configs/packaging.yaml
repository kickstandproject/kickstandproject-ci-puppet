# This file is generated by Puppet
#  DO NOT EDIT THIS FILE BY HAND -- YOUR CHANGES WILL BE OVERWRITTEN
#
- job-template:
    name: 'gate-{name}-buildpackage'
    concurrent: false

    triggers:
      - zuul

    builders:
      - gerrit-git-prep
      - buildpackage

    publishers:
      - dpkg-buildpackage:
          artifacts: 'debian/tmp/{name}_*'
      - console-log

    node: '{node}'

- job-template:
    name: 'gate-{name}-pbuilder-{arch}'
    concurrent: false

    triggers:
      - zuul

    builders:
      - copyartifact:
          project: 'gate-{name}-buildpackage'
          filter: 'debian/tmp/{name}_*'
      - shell: |
          rm -rf result
          mkdir result
          sudo /usr/local/bin/pbuilder-{node}-{arch} build --buildresult "`pwd`/result" debian/tmp/{name}_*.dsc
          # Remove copied artifact
          rm -rf debian

    publishers:
      - pbuilder:
          artifacts: 'result/*'
      - console-log

    node: '{node}'

- job-template:
    name: 'ppa-{name}-experimental'

    triggers:
      - zuul-post

    builders:
      - copyartifact:
          project: 'gate-{name}-buildpackage'
          filter: 'debian/tmp/{name}_*'
      - shell: |
          dput ppa:ksp-apus/experimental debian/tmp/{name}_*.changes
          rm -rf debian

    publishers:
      - console-log

    node: '{node}'
